# Analysis: F1.S2 — Task CRUD Service Layer

**Date:** 2026-04-07
**Analyzer:** Captain
**Plan:** `20260407-f1s2-task-crud-service`

## Phase 1: Codebase Analysis

### F1.S1 Dependency Verification

**Status:** ✅ COMPLETED

Evidence:
- Task model exists: `/home/deti/taskmanager/src/taskmanager/models.py`
  - SQLAlchemy ORM model with all required fields
  - Unique constraint on `name`
  - UUID primary key auto-generation
  - Timestamps with auto-population
- Database layer exists: `/home/deti/taskmanager/src/taskmanager/database.py`
  - `Base` declarative class
  - `get_engine()` function
  - `get_session_factory()` function
  - `get_db()` context manager
- Tests exist: `/home/deti/taskmanager/tests/test_database.py`
  - Table creation tests
  - Task CRUD tests
  - Unique constraint tests
  - Timestamp tests

**Conclusion:** F1.S1 is complete. All prerequisites for F1.S2 are in place.

### Missing Components

**To create:**
1. `/home/deti/taskmanager/src/taskmanager/exceptions.py` — does not exist
2. `/home/deti/taskmanager/src/taskmanager/services/` — directory does not exist
3. `/home/deti/taskmanager/tests/unit/` — directory does not exist

### Crew Ownership Mapping

From `.fleet/crews/*.yaml`:

| File | Crew | Rationale |
|------|------|-----------|
| `src/taskmanager/exceptions.py` | `core-domain` | Owns "domain exceptions" per config line 4 |
| `src/taskmanager/services/__init__.py` | `task-service` | Owns "src/taskmanager/services/**/*.py" |
| `src/taskmanager/services/task_service.py` | `task-service` | Owns "src/taskmanager/services/**/*.py" |
| `tests/unit/test_task_service.py` | `task-service` | Owns "tests/unit/test_task_service.py" |

**Partners:**
- `core-domain` lists `task-service` as partner
- `task-service` lists `core-domain` as partner
- Cross-crew coordination required for exception import

### Technical Constraints

From `CLAUDE.md` and crew configs:

1. **Type safety:**
   - Strict mypy on all new code
   - Complete type annotations required
   - SQLAlchemy 2.x type hints (`Mapped[T]`)

2. **Framework isolation:**
   - Service layer MUST NOT import `typer`, `fastapi`, `pydantic` schemas
   - Pure Python + SQLAlchemy only
   - Session dependency injection (first parameter)

3. **Testing:**
   - Use existing `db_session` fixture from `tests/conftest.py`
   - In-memory SQLite for isolation
   - Target coverage > 95%

4. **SQLAlchemy 2.x:**
   - Use `session.execute(select(...))` NOT `session.query()`
   - ORM queries only, no raw SQL

### Integration Mode

From `.fleet/bridge.yaml`:
- `merge_strategy: local` — crews commit locally, no PRs
- `merge_mode: squash` — one commit per crew
- `ralph_enabled: true` — auto-continuation active

## Phase 2: Risk Assessment

### Low Risk
- Task model already exists and tested
- Database layer stable
- Test fixtures available

### Medium Risk
- Cross-crew coordination (exceptions from core-domain to task-service)
- Wave dependency — Wave 2 blocked until Wave 1 complete

### Mitigation
- Clear handoff protocol in plan
- Changelog documentation required from core-domain
- Decision gate after Wave 1

## Phase 3: Crew Assignment Validation

### Wave 1: core-domain
**Capacity:** Available
**Scope:** Single file (`exceptions.py`)
**Estimated time:** 15 minutes
**Confidence:** High (simple exception classes)

### Wave 2: task-service
**Capacity:** Available
**Scope:** Service layer + tests (3 files)
**Estimated time:** 90 minutes (45 min implementation + 45 min tests)
**Confidence:** High (clear acceptance criteria, existing fixtures)

**Dependency:** Wave 2 must wait for Wave 1 completion

## Recommendations

1. **Proceed with plan as proposed**
   - Dependencies verified
   - Crew ownership clear
   - No conflicts detected

2. **Use decision gate after Wave 1**
   - Verify exception API before continuing
   - Allow for revision if needed

3. **Monitor for framework leakage**
   - Red flag: any import of `typer`, `fastapi` in service layer
   - Enforce in code review

4. **Verification checklist**
   - Run `pytest tests/unit/test_task_service.py -v`
   - Run `make typecheck`
   - Run `make lint`
   - All must pass before marking complete

## Next Steps

1. Present plan to user for approval
2. If approved, spawn crews in wave order:
   - Wave 1: `core-domain`
   - Wave 2 (after gate): `task-service`
3. Monitor task completion via `.fleet/tasks/` status updates
4. Review changelogs in `.fleet/changes/`
5. Execute final verification checklist
6. Generate final report
